import { FilterReciboPipe } from './filter-recibo.pipe';

describe('FilterReciboPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReciboPipe();
    expect(pipe).toBeTruthy();
  });
});
